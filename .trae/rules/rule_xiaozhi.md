# Xiaozhi ESP32 开发规则

> **项目**: Xiaozhi ESP32 AI 罗盘项目  
> **版本**: v1.0  
> **更新日期**: 2026-06-15

---

## 概述

本文档定义了 AI 罗盘项目的开发流程和规范，确保每次功能迭代都能正确编译、烧录和验证。

## 开发流程规则

### 1. 功能迭代流程

每完成一次功能迭代都需要遵循以下流程：

1. **编译和烧录** - 确保新的功能正常运行
2. **功能验证** - 真机测试所有功能是否正常
3. **人工确认** - 直到所有功能都正常运行，待人工确认之后再进行下一步迭代
4. **状态更新** - 每个功能迭代完成之后要及时更新最新状态，包括功能描述、代码注释等

### 2. 问题处理

如果在开发过程中发现新的问题或错误：

1. **及时修复** - 发现新的问题或错误，要及时修复
2. **更新状态** - 修复完成后及时更新状态文档
3. **验证确认** - 修复后重新编译烧录验证

### 3. 编译和烧录规范

> **重要**: 所有的编译和烧录都需要使用项目中固定的 `build_and_flash.sh` 脚本进行，不能使用其他方式。

#### 为什么必须使用 `build_and_flash.sh`？

- **错误信息完整性** - 不要使用 `2>&1` 重定向，否则会导致编译和烧录过程中输出的错误信息被忽略
- **环境一致性** - 使用固定脚本确保编译环境一致
- **流程标准化** - 统一的编译烧录流程减少出错概率

#### 使用方式

```bash
# 在项目根目录下执行
./build_and_flash.sh
```

或者根据脚本内容指定参数：

```bash
./build_and_flash.sh <board_type> <port>
```

> **注意**: 如果脚本不存在或有问题，请先检查脚本内容或创建符合项目需求的编译烧录脚本。

---

## 代码规范

### 1. 功能描述

每个功能的代码应该包含以下描述：

- **功能名称** - 清晰的功能名称
- **功能说明** - 功能的详细说明
- **参数说明** - 输入输出参数的描述
- **使用示例** - 代码使用示例

### 2. 代码注释

- **关键逻辑** - 必须注释复杂的业务逻辑
- **参数含义** - 重要的参数需要有注释说明
- **TODO/FIXME** - 使用 TODO 标记待完成的功能，FIXME 标记需要修复的问题

### 3. 文档更新

每次功能迭代完成后，需要更新以下文档：

- **README.md** - 项目说明文档
- **产品功能与技术实现方案** - 详细的功能和技术文档
- **代码注释** - 代码中的注释和说明

### 4. 自动化验证闭环

每次烧录完成后，必须进入以下自动化验证闭环，直到截图分析结果与预期显示一致：

1. **后台运行截图脚本** - 每次烧录完成后，在后台运行设备截图脚本。
2. **自动解析最新截图** - 解析最新截图，并自动触发待验证的功能按钮后再次截图。
3. **截图分析对比** - 分析触发后的截图，判断是否与预期显示结果一致。
4. **进入闭环迭代** - 若不一致则修复后重新编译烧录，重复上述步骤，直到分析结果与预期显示结果一致。

### 5. 截图验证工具规范

#### 脚本位置
`tools/screenshot_with_log.py`

#### 支持的命令模式

| 模式 | 命令 | 说明 |
|------|------|------|
| 连续截图 | 默认 | 连续获取最多N张截图 |
| 单次截图 | `--snap` | 发送SNAP命令触发单次截图 |
| 按钮触发 | `--click <index>` | 触发功能按钮并获取截图 |

#### CLICK按钮索引定义

| 索引 | 功能 | 触发动作 |
|------|------|----------|
| 0 | 今日运势 | 显示综合运势界面 |
| 1 | 财富运势 | 显示财富运势界面 |
| 2 | 健康运势 | 显示健康运势界面 |
| 3 | 今日求财 | 显示求财界面 |

#### 标准验证流程

```bash
# 1. 烧录完成后，测试基础截图功能
python3 tools/screenshot_with_log.py --snap

# 2. 触发"今日运势"按钮并验证界面显示
python3 tools/screenshot_with_log.py --click 0

# 3. 触发"财富运势"按钮并验证界面显示
python3 tools/screenshot_with_log.py --click 1

# 4. 触发"健康运势"按钮并验证界面显示
python3 tools/screenshot_with_log.py --click 2
```

#### 输出文件规范

| 文件路径 | 说明 |
|----------|------|
| `screenshots/screenshot_latest.jpg` | 最新截图（覆盖保存） |
| `screenshots/history/screenshot_<时间戳>_<序号>.jpg` | 历史截图存档 |
| `screenshots/logs/run_<时间戳>.log` | 运行日志 |

#### 常见问题处理

- **串口被占用**: 使用 `lsof /dev/cu.usbmodem1101` 查找进程并 `kill -9 <PID>`
- **截图数据不完整**: 增加等待时间或检查设备负载
- **CLICK无响应**: 确认UART接收缓冲区大小（建议≥8192字节）

---

## 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | 2026-06-15 | 初始版本，定义开发流程规范 |
| v1.1 | 2026-06-18 | 修正第 4 节"自动化验证流程"格式（拆分 4 步列表为独立条目），补全编号层级 |
| v1.2 | 2026-06-19 | 新增第 5 节"截图验证工具规范"，定义 CLICK 按钮索引、标准验证流程、输出文件规范及常见问题处理 |

---

*本文档由 AI 助手自动整理生成*
